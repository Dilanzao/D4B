{
  "name": "dofus4business",
  "private": true,
  "version": "2.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview",
    "validate": "node scripts/validate-project.js",
    "check": "npm run generate:catalog && npm run validate:catalog && npm run test && npm run test:dom && npm run validate",
    "generate:catalog": "node scripts/generate-catalog.js",
    "validate:catalog": "node scripts/validate-catalog.js",
    "test": "node scripts/smoke-test.js",
    "test:dom": "npm run build --silent && node scripts/dom-smoke-test.js"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "vite": "8.1.4"
  },
  "dependencies": {
    "chart.js": "4.4.9"
  }
}
